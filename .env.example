# Sanity Configuration
# Get your Project ID from Sanity dashboard: https://manage.sanity.io/

# Next.js uses NEXT_PUBLIC_ prefix for client-side environment variables
NEXT_PUBLIC_SANITY_PROJECT_ID=2b0anlve
NEXT_PUBLIC_SANITY_DATASET=production

# Server-side only (don't expose to client)
SANITY_PROJECT_ID=YOUR_PROJECT_ID_HERE
SANITY_DATASET=production
SANITY_API_TOKEN=YOUR_API_TOKEN_HERE  # Optional: needed for content updates via API

# Optional: Sanity API version (defaults to latest)
# SANITY_API_VERSION=2024-01-01

############################################
# Gemini / Chat Assistant (add your keys)
############################################
# Add your Gemini (or Google AI) API key below in your local .env.local file.
# Example (paste your real key into .env.local, DO NOT commit):
# GEMINI_API_KEY=sk-xxxxxxxxxxxxxxxxxxxxxxxxxxxx
# GEMINI_API_URL=https://api.your-provider.com/v1/generate

# Alternate env names accepted by the server route:
# GOOGLE_API_KEY=your_google_api_key_here
# GOOGLE_API_URL=https://api.your-provider.com/v1/generate

# Notes:
# - Copy this file to `.env.local` and replace placeholder values with real secrets.
# - Never commit `.env.local` to source control. Add it to .gitignore if necessary.
